const getRndInt = (min, max) =>
  Math.floor(Math.random() * (max - min + 1)) + min;

const x = getRndInt(99, 100);
const y = getRndInt(1, 50);

// console.log(x);
// console.log(y);

console.log(randomInput);